# ChatGPT identity, accounts and Codex injection: implementation spec

Realizes [ADR-068](adr/adr-068_chatgpt-identity-vault-owned-codex-injection.md). Five reviewed slices under [ADR-026](adr/adr-026_development-workflow.md): the main model writes the kickoff, an Opus implementing agent builds it, the main model reads every changed line, re-runs gates, drives the real app and commits. Mockups reviewed by the owner on 2026-09-13 are the visual target (five screens: providers and accounts, Engines › Codex, Codex on topic pages, per-session account picker, sign-in dialog).

Standing rules for every implementing agent on this spec:

- No `git add`, `commit`, `stash`, `checkout`, `reset`, `branch`, `push`. No `bun install`/`add`/`remove`. No `bun run format` over the repo (format only the files you created or changed, by path). Do not delete or revert any file you did not create. Report the exact file list you touched; the reviewer diffs it against `git status`.
- No real credentials. Never read `~/.claude/ui/auth-vault.json`, `~/.codex/*`, `~/.pi/agent/auth.json` or opencode's `auth.json`. Every test uses temp directories and mocked HTTP; no test may call `auth.openai.com`. Token material never appears in logs, IPC results, test names or fixtures that look real.
- Every behaviour change ships with a guard test the agent proves fails before the fix and passes after. Report both runs.
- Report results and deviations; never self-certify.

## Slice 1: vault accounts

**Landed 2026-09-13** on `codex-integration` (the commit after `e23d5f93`). Built as specified; the points where the build refined the design:

- A migrated v2 account's id is DERIVED (`sha256(providerId|workspaceId-or-email)` prefix), not random: the migration runs on every read until the next write, so two reads must agree on the id.
- A credential with no workspace claim matches the active account only when that account has none either (the single slot a v2 migration leaves); it never overwrites an identified account.
- `saveCredential` routes every OAuth record to `upsertAccount` (the vault has no provider-kind lookup; in v3 `credentials` holds API keys only, so the rules coincide).
- `removeAccount` rings `onActiveAccountChanged` on both paths that change what the engines hold (promotion, and last-account removal), not only `switchActiveAccount`; a review found the spec's wording left opencode holding a removed credential (ADR-047).
- The Add sheet keeps its ChatGPT sign-in control once accounts exist, relabelled "Add another account"; the original gate hid it on `connected`, which made "+ Add account" a dead end. Found in review, fixed before commit.
- The Manage sheet renders from `entry.accounts` on the registry row (present but empty on a disconnected ChatGPT row); `provider-account:list` exists for Slice 2's session picker.
- Real-app evidence (isolated temp profile via an `os.homedir()` shim passed with Electron's `-r`, never the owner's home): two fabricated accounts render with the active radio, switch re-vends and moves `activeId`, the per-session toggle persists to `providers/chatgpt.json`, removing the active account promotes the other and hides the toggle; a v2 profile shows its one migrated account and the file stays `v: 2` until the first write.

### Goal

The vault holds N ChatGPT accounts with one active account. `CredentialSync` refreshes every account and vends the active one to pi and opencode exactly as today. The provider registry projects the accounts onto the ChatGPT row, and the Manage sheet shows an Accounts card (radio for active, Remove, Add) and a Per-session accounts toggle. No Codex behaviour changes in this slice.

### Read first

`docs/adr/adr-068_chatgpt-identity-vault-owned-codex-injection.md` §1–2, `docs/adr/adr-036_unified-auth-vault.md`, `docs/adr/adr-037_shared-provider-routing-and-plaintext-vault.md`, `src/core/auth/vault/AuthVault.ts`, `src/core/auth/vault/codex-oauth.ts` (the `VaultCredential` and `JwtClaims` shapes, `exchangeCodeForTokens`), `src/core/auth/vault/CredentialSync.ts` (all of it; the scheduler, reconcile, feed and watcher sections), `src/core/shared-providers/SharedProviderRepository.ts`, `SharedProviderService.ts`, `provider-registry.ts`, `src/shared/shared-provider.ts`, `src/shared/provider-registry.ts`, `src/core/ipc/auth-commands.ts`, `src/renderer/src/components/SettingsDialog/ProviderSheet.tsx` (`credentialRows` and the Enabled-for group), `ProviderList.tsx`, and the existing tests under `src/main/auth/vault/__tests__/`, `src/core/shared-providers/__tests__/`, `src/renderer/src/components/SettingsDialog/__tests__/ProviderSheet.component.test.tsx`, `src/main/ipc/__tests__/remote-handlers.ipc.test.ts` (the pinned remote channel set).

### Design (decided; do not reopen)

**Vault file v3.** `{ v: 3, credentials: Record<providerId, {type:'api_key', key}>, accounts: Record<providerId, { activeId: string | null, list: VaultAccount[] }> }`. `credentials` keeps only API-key records for custom providers; OAuth subscriptions live in `accounts`. `VaultAccount = { id: string (random, stable), email?: string, accountId?: string (ChatGPT workspace id from the JWT), planType?: string, credential: VaultCredential, addedAt: number }`. Read-time migration: a v2 file with a `chatgpt` OAuth credential becomes one account (id minted, `email`/`accountId` copied) that is active; a v1 plaintext file goes through the existing v1→v2 path first. Malformed entries are dropped as today. Write is the same atomic 0600 rename.

`VaultCredential` gains optional `planType`, parsed at exchange and refresh from the id_token claim `https://api.openai.com/auth`.`chatgpt_plan_type` (same claim sources and order `parseJwtClaims` / `accountIdFromClaims` already use for the account id). Do not change the OAuth constants.

**AuthVault API.** Keep `load()`/`save()` as the ACTIVE account's credential (compatibility for `CredentialSync` feed targets and `getStatus`). Add `listAccounts(providerId)`, `getActiveAccountId(providerId)`, `setActiveAccount(providerId, id)`, `upsertAccount(providerId, credential)` (match by `accountId`: same workspace updates the stored account in place and keeps its id; a new workspace appends; the first account ever becomes active; a re-login of the active account keeps it active), `removeAccount(providerId, id)` (removing the active account promotes the most recently added remaining one, or leaves `activeId` null), `saveAccountCredential(providerId, id, credential)` for the refresher. Existing `loadCredential`/`saveCredential`/`removeCredential` keep working for API-key providers; `saveCredential` with an OAuth record under a subscription provider must route to `upsertAccount` so no caller can silently write the old single-slot shape.

**CredentialSync.** Refresh is per account: one timer per stored account (15 min before its expiry, the existing retry/give-up chain per account, `needsReauth` becomes per account id). `feedAll` writes the ACTIVE account only; `completeLogin` upserts, and feeds only when the upserted account is the active one. Add `switchActiveAccount(id)`: set active, feed both engine stores from the new active credential, then call the injected `onActiveAccountChanged()` hook (Slice 2 wires it; Slice 1 leaves the default a no-op) so the boot seam can recycle opencode per ADR-047 (`opencodeServerManager.recycleAll()` is what `OpencodeAuthProvider` already calls after every auth-store mutation; the hook lets the boot seam do the same without `CredentialSync` importing opencode). `removeAccount(id)`: stop its timer, remove it; if it was active, feed the promoted account or, if none remains, remove both engine copies (the existing `disconnectChatgpt` cleanup). The reconcile-on-start and fs-watch adoption logic keeps operating on the ACTIVE account only: an engine-rotated token is adopted into the active account when it strictly beats it, never into another account. `getStatus()` gains `accounts: Array<{id, email?, accountId?, planType?, expiresAt, needsReauth}>`, `activeId`, and keeps its existing fields for the active account; still never returns token material.

**Shared provider definition.** `SharedProviderDefinition` gains optional `accounts?: { perSession: boolean }` (default false), persisted in `~/.claude/ui/providers/chatgpt.json` by the repository's `normalizeChatgpt` path. Only meaningful for `kind: 'subscription'`.

**Commands** (in `auth-commands.ts`, capability `config`, registered for both transports; add them to the pinned remote channel set): `provider-account:list` → `{ activeId, perSession, accounts: [...] }` from `credentialSync.getStatus()` plus the definition; `provider-account:switch (providerId, id)`; `provider-account:remove (providerId, id)`; `provider-account:set-per-session (providerId, enabled)`. Adding an account reuses the EXISTING sign-in path untouched (`vendor-auth:oauth-authorize` / `oauth-callback` on engine `pi`, vendor `openai-codex`, which delegates to `credentialSync.beginLogin/completeLogin`) — its completion now upserts. Slice 3 replaces the entry point, not the flow. Every handler is token-free; pin that with a test like the existing `probe`/`list-keys` one.

**Registry projection.** `ProviderEntry` gains optional `accounts?: { activeId: string | null, perSession: boolean, list: Array<{ id, email?, planType?, accountId? }> }`, set on the shared subscription row from a new `ProviderRegistrySources.chatgptAccounts` source (filled by `listProviderRegistry` from `credentialSync.getStatus()`). `credential` stays `'connected'` when at least one account exists. The row `detail` reads `"2 accounts · daniel@example.com active"` when more than one account exists.

**Manage sheet.** For a shared subscription row, the Credential group becomes an **Accounts** card: one `SettingRow` per account (`as="label"`, radio `leading`, label = email or "Account", description = plan and a shortened workspace id, active row tinted, a danger `Remove` button), a header action `+ Add account` that calls the existing `onAddProvider(entry.id)` path, and below the list a `Per-session accounts` toggle row (description per the mockup; hidden when fewer than two accounts). The existing `Disconnect` moves to the sheet footer as `Disconnect all accounts` with the same confirm. `ProviderList`'s `CredentialChip` for a multi-account row says `N accounts`. Follow ADR-027 testids: `ProviderSheet.account` with `dataId` = account id, `ProviderSheet.accountRemove`, `ProviderSheet.addAccount`, `ProviderSheet.perSession`, `ProviderSheet.disconnect`.

Mobile: the sheet already renders inside the mobile settings view; verify the new rows read at 390px in the component test (no new mobile code expected).

### Guards (each a test that fails before the change)

1. v2 → v3 migration: a v2 chatgpt credential becomes one active account with email/accountId preserved; a v2 API-key custom provider stays under `credentials`.
2. `upsertAccount`: same workspace updates in place and keeps id and active flag; new workspace appends and does not steal active; first account becomes active.
3. `removeAccount` of the active account promotes the newest remaining and feeds it; removing the last removes both engine copies.
4. Two accounts, two expiries: each is refreshed on its own schedule; only the active one is fed; a revoked refresh sets `needsReauth` for that account only.
5. fs-watch adoption updates the active account only.
6. `getStatus`, `provider-account:list` and the registry snapshot contain no `access`/`refresh` strings.
7. Registry: the ChatGPT row carries `accounts` with the right active id and `detail`.
8. ProviderSheet: renders N account rows, radio switch calls `provider-account:switch`, Remove calls remove, toggle calls set-per-session and is hidden with one account.
9. The pinned remote channel set includes the four new channels.

### Gates before handing back

`bun run typecheck`, `bun run lint`, `bun run test` (full default run, not filtered), and the format check on changed files only. Report each command's exact result. Then the reviewer drives the real app: Settings › Models & providers › ChatGPT › Manage shows the Accounts card against a temp `HOME` with a fabricated v2 vault (the reviewer prepares that; the agent does not touch the real vault).

### Out of scope for Slice 1

Codex injection, the account picker in the input bar, `capabilities.auth.perSessionAccount`, the sign-in dialog, removing the device-code UI, rate limits. Do not add `codex` to `ConfigurableHarnessId` yet.

## Slice 2: Codex injection

Split into 2a (the credential path) and 2b (per-session pinning, picker, rate limits) so each is one reviewable diff.

### Slice 2a: inject the vault's ChatGPT token into every Codex process

**Landed 2026-09-13** on `codex-integration` (the commit after `855755af`). Built as specified, with these refinements found in review and in the first real-binary run:

- The hook is opt-in at every composition root (`register-engines.ts`, `CodexAuthProvider`, `model-discovery.ts`, the `crossEngineDispatcher` singleton); a session, service or dispatcher built without one injects nothing and never reads the vault, which keeps the real-binary fixtures hermetic.
- `injectionTokenFor(accountId, refreshMarginMs)`: the inject half uses the 15-minute margin, the refresh half passes 0 so a still-valid cached token answers Codex inside its 10 s deadline.
- `CodexTransportError` carries `nativeMessage` for `rpc-error-*` rejections; only the injection path reads it (the refusal text is the product message). A thrown `inject()` is fatal to the session start, never a fall-through to native auth.
- `probe()` reports a revoked active account as `unauthenticated` + `requiresLogin` (ADR-030), not as a healthy subscription.
- `session:auth-required` is a canonical, ringed domain event (`authRequired` on the session, cleared when a turn runs again) but not yet on the wire; Slice 3 adds the snapshot field with the UI that renders it.
- The generator's schema check unwraps the nullable `anyOf` form `account/rateLimits/read` declares for its params; it still pins the params type by name.
- **The real-binary proof ran on Windows x64** (`src/integration/codex/codex-injection.integration.test.ts`, now gated for macOS arm64 and Windows x64; on Windows the child runs without the seatbelt and the fixture isolates by a replacement environment): injection accepted, `account/read` reports a `chatgpt` account with the fake email, native logins refused with "External auth is active", no `auth.json` written; a scripted 401 produced exactly one `account/chatgptAuthTokens/refresh` carrying the workspace id, the retry carried the rotated token and the turn completed.
- **Findings from that run.** (1) The binary reaches `chatgpt_base_url` (rate limits, `wham/settings/user`) and `GET /v1/models` on its own; the fixture now points `chatgpt_base_url` at itself and answers both 404, because a fake token hitting the real backend earns 401s and unscripted refresh requests. (2) A USER-level `forced_chatgpt_workspace_id` in `config.toml` did **not** gate the injected login on 0.154.0; the test pins the observed behaviour and the refusal path stays wired (unit-tested with a scripted native error). ADR-068 §1's "surfaced verbatim" therefore applies when Codex refuses, which on this binary means a managed `allowed_chatgpt_workspaces` policy.

**Goal.** Every app-server ClaudeUI starts receives the vault's active ChatGPT account through `account/login/start { type: 'chatgptAuthTokens' }` right after `initialize`, answers the app-server's `account/chatgptAuthTokens/refresh` from the vault, attributes the session to the vault account, and drops the native device-code product UI. No per-session pinning yet: the ACTIVE account is what every process gets.

**Read first.** ADR-068 §1 and §4; `docs/protocol-codex/README.md` (regeneration and the real-binary fixture); `src/core/codex/CodexAppServerClient.ts` (`CodexClientOptions.serverMethods`, `onServerRequest` and its `context`), `CodexClient.ts`, `CodexSession.ts` (`start()`, the `serverMethods` const, `requestApproval` dispatch, `this.account`), `CodexService.ts` (`initialize`, `read()`, `accountStatus()`, `startLogin`), `model-discovery.ts`, `src/core/services/cross-engine-dispatcher.ts` (`SpawnCodexTargetFn`, `resolveAndRunCodex`, where the target client is started), `src/core/auth/CodexAuthProvider.ts`, `src/core/ipc/codex-commands.ts`, `src/core/auth/vault/CredentialSync.ts` (Slice 1's per-account runtime, `loadForKey`, `runRefresh`), `scripts/generate-codex-protocol.mjs` (the `methods` / `serverMethods` / `notifications` selection maps), the Codex source at `.cache/codex-src/codex-rs/app-server/src/external_auth.rs` and `request_processors/account_processor.rs` (`login_chatgpt_auth_tokens_response`), `src/integration/codex/` (the real-binary fixture and how it isolates `CODEX_HOME`), `src/renderer/src/components/SettingsDialog/CodexAccount.tsx` and its page entry in `settings-pages.tsx`.

**Design (decided; do not reopen).**

1. _Protocol._ Add to the generator's selection: `serverMethods['account/chatgptAuthTokens/refresh'] = ['ChatgptAuthTokensRefreshParams', 'ChatgptAuthTokensRefreshResponse']`, `methods['account/rateLimits/read'] = ['GetAccountRateLimitsParams', 'GetAccountRateLimitsResponse']`, `notifications['account/rateLimits/updated'] = 'AccountRateLimitsUpdatedNotification'`. Regenerate with `bun run generate-codex-protocol` and confirm `bun run check-codex-protocol` passes; never hand-edit generated files. Windows x64 has the pinned binary vendored.

2. _Token source._ `CredentialSync` gains `async injectionTokenFor(accountId: string | null): Promise<{ accessToken: string; chatgptAccountId: string; chatgptPlanType: string | null; vaultAccountId: string } | null>`. `null` accountId means the active account. Returns null when the vault holds no account for ChatGPT or the account has no workspace id (Codex requires `chatgptAccountId`). If the credential's `expires` is within `REFRESH_MARGIN_MS`, it runs that account's `runRefresh(key)` first (single-flight, per account) and re-reads. It never logs, never appears in `getStatus()`, and is the ONLY method on the class that returns token material; mark it so in its doc comment and add a test that `getStatus()` / `provider-account:list` are unchanged.

3. _Inject after initialize._ `CodexClient.start(params, auth?: CodexAuthHook)` where `CodexAuthHook = { inject(): Promise<InjectionToken | null>; onRefreshRequest(params): Promise<ChatgptAuthTokensRefreshResponse> }`. After the transport handshake, `start` calls `inject()`; when it returns a token it sends `account/login/start { type: 'chatgptAuthTokens', accessToken, chatgptAccountId, chatgptPlanType }` and awaits the response; a rejection is thrown as `CodexInjectionError` carrying the native message verbatim (this is where `forced_chatgpt_workspace_id` surfaces) — no retry, no fallback to native auth. When `inject()` returns null the process is left on whatever Codex's own store holds (a user who never signed in through ClaudeUI keeps working), and the caller records `account = null`. Register `account/chatgptAuthTokens/refresh` in each caller's `serverMethods` and route it to `onRefreshRequest`. The handler answers from `injectionTokenFor(...)` — the app-server sends the WORKSPACE id in `previousAccountId`, so map it back to the vault account whose `accountId` equals it, falling back to the account that was injected into this process, then to active — and must resolve well inside Codex's 10 s deadline: answer from the cached credential unless it is expired. On failure (no account, refresh revoked) reply with a JSON-RPC error and emit `session:auth-required { providerId: 'chatgpt', accountId }` on the session (a new domain event; the renderer wiring is Slice 3, so for now the reducer stores it and the existing `session:error` carries a one-line message "ChatGPT sign-in expired; sign in again from Settings › Models & providers").

4. _Callers._ `CodexSession.start()` (record which vault account id was injected in `this.injectedAccountId`), `CodexService` (its read client; `startLogin` stays as tested code but the provider no longer exposes it), `model-discovery.ts` (through the service), and the dispatch target spawn in `cross-engine-dispatcher.ts` (`SpawnCodexTargetFn` gets the hook; the default spawn uses the active account). One shared hook factory in `src/core/codex/codex-auth-hook.ts` so the four callers cannot drift.

5. _Attribution._ `this.account` becomes `{ engineId: 'codex', vendorId: 'openai', authState: 'authenticated', billingType: 'subscription', label: <email from account/read>, accountId: <vault account id> }` when injected; when not injected it keeps today's derivation from `account/read` and `accountId` is absent. `session:auth-required` is added to the domain-event contract in `src/core/shared/sync/` with a reducer case (store on the session as `authRequired: { providerId, accountId? } | null`, cleared on the next successful turn start).

6. _Provider and UI._ `CodexAuthProvider.probe()` reads the vault's active account first (`credentialSync.getStatus()`): with an account it reports `authenticated` / `subscription` / label = email; without one it falls back to the service's native read as today. `loginStart` / `loginStatus` / `loginCancel` and the channels `codex:login-start`, `codex:login-status`, `codex:login-cancel` are deleted (preload, web adapter, boot-test-app, shared API type, the pinned remote channel set); `codex:auth-status` stays as the availability + model-count query. `CodexAccount.tsx` becomes one `SettingRow`: label "ChatGPT · <email>" with a plan badge (or "Not signed in through ClaudeUI" and, when the native read says a native login exists, the description "Codex is using its own login; sign in here to manage it in ClaudeUI"), description "N accounts available", and a link button "Manage in Models & providers ›" dispatching the existing `open-settings` deep link `{ page: 'models', group: 'providers' }`. The Codex page description loses "No shared-vault credentials are copied."

**Guards.**

1. Transport: `start()` sends `initialize`, then `account/login/start` with exactly the injected triple, and resolves only after the login response; a login rejection surfaces as `CodexInjectionError` with the native message and the process is disposed; `inject() → null` sends no login.
2. Refresh: a server request `account/chatgptAuthTokens/refresh` with `previousAccountId = <workspace>` is answered with that account's token; with an unknown workspace it falls back to the injected account; a revoked account answers an error and the session emits `session:auth-required` and one `session:error`.
3. Expiry: an account within the refresh margin is refreshed once (single-flight) before injection; two concurrent `injectionTokenFor` calls cause one network refresh.
4. `getStatus()` and `provider-account:list` still contain no token material after the new method exists.
5. Attribution: `SessionStatus.account.accountId` equals the vault account id after injection.
6. The pinned remote channel set drops the three login channels and keeps `codex:auth-status`.
7. Component: the new `CodexAccount` row renders the three states and its link dispatches the deep link.
8. Integration (real binary, `src/integration/codex/`, isolated `CODEX_HOME`, fake JWT minted in the test with the claims Codex reads: `exp`, `https://api.openai.com/auth.chatgpt_account_id`, `email`; the fixture's mock provider does not verify signatures): (a) injection accepted, `account/read` reports `chatgpt` with the fake email; (b) the mock provider returns 401 once → the app-server sends `account/chatgptAuthTokens/refresh` with the fake workspace id → the hook answers → the turn completes. Prove that `account/login/start { type: 'chatgpt' }` is refused with "External auth is active" afterwards. Run with `bun run test:integration`.

**Gates.** typecheck, lint, full `bun run test`, `bun run test:integration`, `bun run check-codex-protocol`, prettier on changed files. The reviewer then runs the real app against the owner's account (the ONLY step allowed to touch a real credential, and only the reviewer does it): a Codex session starts under injection, `account/read` shows the ChatGPT email, one short turn completes.

**Out of scope for 2a.** Per-session pinning, `capabilities.auth.perSessionAccount`, the account picker, `codex_session_overrides.accountId`, rate limits and the usage panel, the sign-in dialog, MCP, settings groups.

### Slice 2b: per-session pinning, the picker, rate limits

**Landed 2026-09-14** on `codex-integration` (the commit after `786902fd`). Built as specified, with these refinements from review and the live drives:

- The hook gained `requestAccount(id | null)` (a sticky setter, so a later 401 recovers onto the pinned identity, not the previous one) and a token-free `hasAccount(id)`; `CodexClient.injectAccount(hook)` re-points a live process without disposing on refusal.
- The pin never goes through `setCodexSettings` (it is not a native thread setting); `setAccount` persists it itself and `setCodexSettings` refuses an `accountId`. A pin chosen BEFORE the first prompt starts the process, exactly as a pre-spawn model or effort pick does — parking it left the picker reading "Active · …" until the first turn (seen live).
- `CodexService.rateLimits(list)` takes the whole account list so one process serves every account in turn; per-account calls would have spawned one child each.
- `resetsAt` is unix SECONDS (`protocol/src/protocol.rs`, converted verbatim into the v2 type); converted to ISO 8601 on the way in.
- Codex→Codex dispatch is refused by `dispatchInner` (same engine as the caller), so the "caller pin reaches the target" wiring is exercised only by its two halves' tests; it is kept because the hook factory needs the account argument regardless and the refusal may be lifted later.
- The picker's account list is re-read on InputBox mount and whenever the menu opens, rather than after settings writes (the input bar stays decoupled from the settings surface).
- A refused pin surfaces through the session's error row (`addError`), not an unhandled rejection.
- **Live findings.** A business workspace answers `account/rateLimits/read` with no `primary`/`secondary` windows in either view and only `credits`, so the usage block shows credits when that is what the plan reports, and reads `rateLimitsByLimitId.codex` when the top-level view is empty. Verified in the real app against the isolated two-account profile: the picker appears once the toggle is on, pinning the second account spawned the process under it (label and `pinnedAccountId` moved, menu check moved), and "Follow active account" re-injected the first; against the owner's profile the ChatGPT block appears with email and plan.

**Goal.** A Codex session can pin one stored ChatGPT account (or follow the active one), the pin survives turns, resumes and forks, dispatch targets run under the caller's pin, and the sidebar usage panel shows ChatGPT rate limits per account. Depends on 2a's `CodexAuthHook` / `injectionTokenFor`.

**Read first.** ADR-068 §2; Slice 2a as built (`git log` for its commit and the files it touched); `src/core/codex/settings.ts` (`parseCodexSettings`, `savedCodexOverrides`), `src/core/services/db.ts` (`codex_session_overrides` is a JSON blob in `settings_json` — a new key is NOT a schema migration), `CodexSession.ts` (`setCodexSettings`, `setPermissionMode`'s "applies from the next turn" rule, `start()`), `src/core/providers/ISession.ts`, `src/core/ipc/session.ipc.ts` and `remote-handlers.ts` (`session:set-model` / `session:set-effort` registrations and their `setModel` / `setEffort` helpers), `src/shared/model-capabilities.ts` (`auth: { canDriveLogin, multiAccount }` per engine), `src/renderer/src/components/shared/InlinePickers.tsx` (`EffortPicker` with `nativeOptions`), `src/renderer/src/components/chat/InputBox/InputBox.tsx` and `View.tsx` (how `nativeEffortOptions` flows), `MobileConfigSheet.tsx` (its `Page` union), `src/renderer/src/components/Sidebar/UsagePanel.tsx` (`UsageProgressBar`, `AccountUsage`, `RateWindow`), `src/core/services/usage-fetcher.ts`, the generated `GetAccountRateLimitsResponse` / `RateLimitSnapshot` / `RateLimitWindow` / `AccountRateLimitsUpdatedNotification` types from 2a.

**Design (decided; do not reopen).**

1. _Pin storage._ `CodexSettings` gains `accountId?: string | null` — a string pins a vault account, an explicit `null` clears the pin ("follow active"), absent leaves it alone. `parseCodexSettings` accepts the key (string or null); `savedCodexOverrides` reads a string back. Stored in `codex_session_overrides.settings_json` beside model and effort; no db migration.

2. _Capability._ `EngineCapabilities.auth` gains `perSessionAccount: boolean` — true for Codex, false for the other three. Declared in `model-capabilities.ts` alongside `canDriveLogin` / `multiAccount`.

3. _Command._ `ISession.setAccount?(accountId: string | null): Promise<void>`. New engine-neutral channel `session:set-account` (capability `session-config`, `sessionIdArg: 0`, both transports, added to the pinned remote channel set) that refuses with a clear error when the session's `capabilities.auth.perSessionAccount` is false. `CodexSession.setAccount`: validate the id against `credentialSync.getStatus().accounts` (unknown → error, nothing written); persist via `setCodexSettings({ accountId })`; if the session is idle re-inject NOW with `account/login/start { type: 'chatgptAuthTokens' }` on the live client (allowed while external auth is active — 2a's hook exposes `inject(accountId)`), then re-read `account/read` and update `this.account` + emit status; if a turn is running, store the pending pin and re-inject at the next turn boundary before `turn/start`, mirroring `setPermissionMode`'s next-turn rule. `null` re-injects the active account. The refresh handler from 2a answers for the PINNED account when one is set (its `previousAccountId` mapping already prefers the injected account).

4. _Start, resume, fork._ `start()` reads the saved pin and injects that account; a pinned account that no longer exists injects the active account instead, clears the pin in the overrides, and emits one `session:error` "The pinned ChatGPT account was removed; this session now follows the active account." Forks copy the source's overrides today, so the pin is inherited without new code — pin that with a test.

5. _Dispatch._ `resolveAndRunCodex` passes the CALLER session's `account.accountId` (when the caller is a Codex session with a pin) into `SpawnCodexTargetFn`'s hook so the headless target runs under the same account; callers on other engines or without a pin use the active account.

6. _Picker._ `AccountPicker` in `InlinePickers.tsx`, same visual grammar as `EffortPicker`: trigger shows the pinned account's email (or "Active · <email>" when following), menu lists "Follow active account" first (with the current active email as description) then every stored account (email, plan) with a radio-style check on the current choice, and an "Add account…" item that opens Settings › Models & providers via the `open-settings` deep link. Shown only when `capabilities.auth.perSessionAccount` is true AND the ChatGPT definition's `accounts.perSession` is on AND at least two accounts exist. Data: a store slice `providerAccounts: SharedProviderAccountList | null` loaded from `provider-account:list('chatgpt')` on InputBox mount and re-read after any `provider-account:*` write and after the sign-in flow completes. The current choice comes from a new `SessionStatus.codex.pinnedAccountId: string | null` — it cannot be derived from `account.accountId`, because a session following the active account carries that same id. `MobileConfigSheet` gets an `account` page with the same list. Selecting calls `window.api.setSessionAccount(sessionId, id | null)`.

7. _Rate limits._ Core keeps `ChatgptRateLimits`: `Record<vaultAccountId, { email?: string; planType?: string; primary: RateWindow | null; secondary: RateWindow | null; fetchedAt: number }>`. `RateLimitWindow.usedPercent` maps to `RateWindow.usedPercent`; `resetsAt` is a unix timestamp in SECONDS in Codex's payload (verify against the generated type's doc and the Rust source; convert to ISO 8601 for `RateWindow.resetsAt`). Two sources: (a) on-demand `CodexService.rateLimits(vaultAccountId)` — inject that account into the service's read client, `account/rateLimits/read`, under the service's existing read serialization (one process, sequential re-injection; never a process per account); (b) live sessions forward `account/rateLimits/updated` into the same map under their injected account id. A `config` query `usage:chatgpt-limits` returns the map; a domain event `usage:chatgpt-limits-changed` (no payload) tells clients to re-query. `UsagePanel` gets a "ChatGPT" section: one block per account (email · plan), `UsageProgressBar` for the 5-hour (primary) and weekly (secondary) windows using the existing `getUsageColor` / `formatResetTime`, and the panel's Refresh also triggers the per-account read. Rate limits are read only when the panel is open or refreshed, plus the live pushes; no polling timer.

**Guards.**

1. `parseCodexSettings` accepts `accountId` string and null, rejects other shapes; `savedCodexOverrides` restores the pin.
2. `setAccount` on an idle session re-injects immediately and updates `account.accountId` + `codex.pinnedAccountId`; on a busy session it defers to the next turn start (assert the login is sent before `turn/start`); an unknown id errors and writes nothing; on an engine without the capability the command refuses.
3. Resume with a removed pinned account: active account injected, pin cleared, exactly one `session:error`.
4. Fork inherits the pin (existing override copy).
5. Dispatch: a Codex caller with a pin spawns the target with that account; a Claude caller spawns with active.
6. Picker: hidden when capability false, toggle off, or one account; shows the right current choice; selecting sends `session:set-account` with the id or null; "Add account…" dispatches the deep link. Mobile page renders the same list.
7. Rate limits: `resetsAt` conversion pinned against a known value; per-account read re-injects then reads; a live `account/rateLimits/updated` updates the map under the session's account and fires the change event; the panel renders one block per account with both bars.
8. Integration (real binary, isolated fixture): `setAccount` on a live thread switches `account/read` to the second fake account's email between turns, and a turn completes afterwards.

**Gates.** typecheck, lint, full `bun run test`, `bun run test:integration`, prettier on changed files. Reviewer: real-app drive against the isolated two-account profile — toggle on, picker visible, switch pin, sidebar shows two ChatGPT blocks (fake accounts will show no rate-limit data; the layout and the "unavailable" state are what is checked).

**Out of scope for 2b.** The sign-in dialog and auth-required card (Slice 3), MCP (Slice 4), settings groups (Slice 5), Claude per-session pinning.

## Slice 3: sign-in dialog and one auth-required event

**Landed 2026-09-14** on `codex-integration` (the commit after `81822982`). Built as specified, with these refinements:

- The store slice is `signInDialog` (the store already had a `signIn()` action, the Anthropic driver the dialog calls). The dialog is a keyed remount per request, so no stage survives a reopen.
- `SheetFrame` is not used (its geometry pins to the settings box); `SettingRow` / `SheetGroup` / `Button` are, so account rows read as in Settings. On a phone the dialog is full-screen.
- opencode emits `session:error` with the vendor's verbatim message alongside `session:auth-required`, mirroring Codex, because the event itself carries no text. Its provider id is `chatgpt` only while the shared ChatGPT route to opencode is enabled and names that vendor (`chatgpt-route.ts`); otherwise `opencode:<vendor>`, whose row opens Settings › Models & providers.
- `authRequired` is sealed and on the snapshot, so a client-side dismissal is a local replica patch that a full resync can resurrect until the next running turn. Accepted: the row is informative, and the field's single writer is the reducer.
- The SERVICE path: `CodexAuthProvider.probe()` now reads the catalog under the injected identity and reports `unauthenticated` when it is refused or empty; the "No Codex models" banner asks that probe first and, when it says so, shows "ChatGPT rejected the credential Codex runs under…" with a Sign in button that opens the dialog (seen live against a profile whose active account carried an unparseable token).
- pi has no distinguishable authentication error on its wire (one `error` output, free text, no class or vendor), so it emits no `session:auth-required`; confirmed by reading the event mapper and the RPC doc.
- `ProviderSheet.onAddProvider` is gone: both its call sites open the dialog. `VendorOAuthFlow` remains for opencode-native vendor OAuth only.
- Live evidence (isolated two-account profile): the dialog lists both accounts with Re-authorize on the active one and Switch on the other, Switch moves the active account and closes; the bad-token profile's banner carries Sign in and opens the dialog on both accounts.

**Goal.** One modal, `SignInDialog`, is the only place a sign-in flow renders, for both providers ClaudeUI drives (Anthropic through cli.js, ChatGPT through the vault). Every entry point opens it; no banner, transcript row, card or settings row carries flow UI any more. Every engine that can tell a rejected credential apart raises the same `session:auth-required`, which reaches clients and renders as one transcript row. Visual reference: mockup screen 5 (owner-approved 2026-09-13).

**Read first.** ADR-068 §3–4, ADR-014, ADR-057 (paste-back; the host always exchanges), ADR-015 (Claude accounts); `src/renderer/src/components/chat/AuthBanner.tsx`, the `AuthErrorBlock` inside `chat/MessageBubble.tsx`, `chat/VendorAuthRequiredCard.tsx`, `auth/OAuthPasteBackFlow.tsx`, `SettingsDialog/VendorOAuthFlow.tsx`, the `AccountsSetting` in `SettingsDialog/settings-sections.tsx`, `ProviderSheet.tsx` / `ProviderAddSheet.tsx` (the ChatGPT sign-in rows Slice 1 left), the store's `signIn` / `submitOAuthCode` / `cancelSignIn` / `authState` (Anthropic) and `authorizeVendorOAuth` / `submitVendorOAuthCode` / `cancelVendorOAuth` / `vendorOAuth` (ChatGPT via pi's `openai-codex`), `AuthFlowState` and `VendorOAuthState`; `src/main/services/account-manager.ts` (`addAccount` returns `pendingSignIn` for a remote caller), `src/core/ipc/auth-commands.ts`; `src/core/shared/sync/{channels,events,reducer,state}.ts` (`session:auth-required` from 2a, `session:vendor-auth-required`), `src/shared/remote-protocol.ts` (`PerSessionSnapshot`), `OpencodeSession.ts` (the `auth-required` output case), `src/core/services/claude-session.ts` (`api_error` with `errorType: 'authentication'`), `CodexSession.ts` (2a's `onAuthRequired`), `SessionView.tsx` (where `SettingsDialog` mounts — the dialog mounts beside it).

**Design (decided; do not reopen).**

1. _One event on the wire._ `session:auth-required { providerId, accountId? }` replaces `session:vendor-auth-required`. `authRequired` joins `PerSessionSnapshot` and the renderer store (replacing `vendorAuthRequired`). Emitters: Codex (2a, `providerId: 'chatgpt'`); opencode's `auth-required` output — `providerId` is `'chatgpt'` when the vendor is the shared ChatGPT route's opencode id (`openai`, and only while that route is enabled), otherwise `opencode:<vendorId>`; Claude's `api_error` with `errorType: 'authentication'` additionally emits `{ providerId: 'anthropic' }` (the error block itself stays in the transcript as data). pi has no distinguishable auth-error signal today — confirm by reading its event mapper and report; do not invent one. The reducer clears `authRequired` when a turn runs again (2a's rule). Also the SERVICE path: when Codex model discovery fails because the injected token is rejected (seen live with a fake token: the generic "No Codex models were discovered" banner), `CodexAuthProvider.probe()` must report `unauthenticated` for that account and the banner must offer Sign in through the dialog instead of the installation hint.

2. _The dialog._ `src/renderer/src/components/auth/SignInDialog.tsx`, mounted once in `SessionView.tsx` beside `SettingsDialog`, driven by a store slice `signIn: { open: boolean; providerId: 'anthropic' | 'chatgpt'; mode: 'reauth' | 'add' | 'switch'; accountId?: string; retry?: { routingId: string; prompt: string } } | null` with actions `openSignIn(...)` / `closeSignIn()`. Three stages as mocked: **choose** (account list for the provider — Anthropic from `getAccounts()`, ChatGPT from `provider-account:list` — with "re-authorize this one", "switch to that one" (calls `switchAccount` / `provider-account:switch` and closes) and "Add another account"; a provider with no stored account skips straight to the flow), **waiting** (the flow panel), **done** (fan-out summary + Retry last prompt when `retry` is set, via the existing `retrySend`). The flow DRIVERS are the existing store actions, untouched: Anthropic = `signIn()` for reauth, `window.api.addAccount()` for add (its `pendingSignIn` seeds the paste panel remotely), `submitOAuthCode`, `cancelSignIn`; ChatGPT = `authorizeVendorOAuth('pi', 'openai-codex')`, `submitVendorOAuthCode`, `cancelVendorOAuth`. The host variant is derived: `window.api.platform === 'web'` shows the paste panel (`OAuthPasteBackFlow` moves inside the dialog; `variant` `'code'` for Anthropic, `'url'` for ChatGPT); desktop shows "Waiting for the browser…" with "Open the link manually" when a URL exists. Dismissing while waiting leaves the flow running (the banner keeps showing its state). Errors render through the existing `OAuthOutcomeNotice`. No device code (headless server is a later slice).

3. _Done-state fan-out._ ChatGPT: "Signed in as <email>" plus one line per enabled route from `provider-account:list` and the definition ("pi and opencode updated; opencode picks it up on its next server start; Codex sessions use the new token on their next request" — 2a's refresh path makes live re-injection lazy, so say that rather than claiming an immediate re-inject). Anthropic: "Signed in as <email> · <plan>". Retry re-sends the captured prompt through `retrySend`.

4. _Entry points, all rewired to `openSignIn`._ `AuthBanner` becomes one line (icon, sentence, "Sign in", "Later"); while a flow is waiting it says so and offers Cancel; it never expands. `AuthErrorBlock` becomes a compact "Turn stopped: <provider> rejected the credential" row with "Sign in" (captures the last user prompt as `retry`) and "Dismiss"; its inline flow states are deleted. `VendorAuthRequiredCard` is deleted; a new engine-neutral `AuthRequiredRow` renders from `session.authRequired` for `anthropic` / `chatgpt` (for an `opencode:<vendor>` provider the row's action opens Settings › Models & providers on that provider instead — API-key vendors have no dialog flow). `AccountsSetting`: "+ Add account" opens the dialog in `add` mode for Anthropic; the `AccountsSetting.signInFlow` paste panel and outcome notice are removed. `ProviderSheet` / `ProviderAddSheet`: the ChatGPT `VendorOAuthFlow` rows become buttons that open the dialog (`add` from "+ Add account" / "Add another account", `reauth` from "Sign in"); `VendorOAuthFlow` itself stays for opencode-native vendor OAuth. The model-picker and welcome-tile entry points from the mock are NOT in this slice (neither surface knows the auth state today); note them as follow-ups.

5. _Store._ `vendorAuthRequired` and `clearVendorAuthRequired` are replaced by `authRequired` / `clearAuthRequired`; the two flow states (`authState`, `vendorOAuth`) stay as they are — the dialog is a view over them, not a third flow state.

**Guards.**

1. Reducer + snapshot: `session:auth-required` sets `authRequired`, a running status clears it, it survives a snapshot round-trip; `session:vendor-auth-required` no longer exists in the channel specs or the event map.
2. opencode maps `openai` → `chatgpt` only while the shared route is enabled, else `opencode:openai`; Claude's authentication `api_error` emits `{ providerId: 'anthropic' }` exactly once per error.
3. Dialog, Anthropic: choose → `signIn()`; web shows the paste panel with the `manualUrl`; `submitOAuthCode` on paste; done shows the email; Retry calls `retrySend` with the captured prompt; add mode calls `addAccount()`.
4. Dialog, ChatGPT: choose lists the vault accounts with the active one marked; "switch" calls `provider-account:switch` and closes; reauth/add call `authorizeVendorOAuth('pi', 'openai-codex')`; web paste calls `submitVendorOAuthCode`; done lists the enabled routes.
5. Banner and rows: the banner never contains a paste field or a URL; the transcript row opens the dialog with `retry` set; `AuthRequiredRow` for `opencode:<vendor>` opens settings instead; `VendorAuthRequiredCard` and the inline `AuthErrorBlock` flow are gone (grep-level test that the old testids no longer render).
6. Settings: `AccountsSetting.addAccount` opens the dialog; `ProviderSheet.addAccount` and the Add sheet's ChatGPT button open the dialog; no `VendorOAuthFlow` renders for ChatGPT.
7. Component test at 390px: the dialog is usable on a phone (buttons reachable, paste panel visible).

**Gates.** typecheck, lint, full `bun run test`, prettier on changed files. Reviewer: real-app drive against the isolated two-account profile (Slice 1's recipe) — open the dialog from the ChatGPT Manage sheet's "+ Add account", from the banner (force it by starting with an empty vault profile) and from a fabricated `session:auth-required` via the verifier hooks; screenshots of all three stages.

**Out of scope for Slice 3.** Device code, the headless-server variant, the model-picker and welcome-tile entry points, pi's auth event, Claude per-session accounts.

## Slice 4: MCP inheritance

**Landed 2026-09-14** on `codex-integration` (the commit after `772d89eb`). Built as specified; the probe answered MERGE in both key forms (`docs/codex-spike.md` § "Merge or replace?"), so the bridge sends the nested `config.mcp_servers` table and native entries keep working. Notes: the three-scope merge moved into `mergeClaudeMcpServers` in `claude-mcp.ts`, shared with the opencode bridge; three existing real-binary integration files mock the bridge so they never spawn a developer's own servers; the SSE warning fires once per `start()` (a resumed session is a new instance and warns again); an inherited server whose name collides with a native `config.toml` entry wins the per-key merge — accepted, because the Claude-scoped list is canonical in ClaudeUI. Live: a real Codex session in a directory whose `.mcp.json` declares a stdio stub spawns it under the real app-server.

**Gate result (2026-09-14, recorded in [codex-spike.md](codex-spike.md) § "Per-thread MCP override probe").** `thread/start.config.mcp_servers` IS honoured by the pinned binary: the override's server was spawned and handshaken, the control was not. The runtime path is therefore the design; the `config.toml`-writing fallback is not needed.

**Goal.** Codex threads start with the same MCP servers Claude and opencode get — the user, project and local Claude MCP configuration (`.mcp.json` / `settings.json`, per-directory disabled list) — translated into Codex's `mcp_servers` shape and delivered as the `config` override on `thread/start`, `thread/resume` and `thread/fork`, without writing anything under `~/.codex`. Native entries the user declared in `config.toml` keep working alongside.

**Read first.** ADR-068 §5; `src/core/opencode/claude-mcp-bridge.ts` (the opencode translation this mirrors: merge order user → project → local, `readDisabledMcpServers(cwd)`, pure translation separated from I/O), `src/core/services/claude-mcp.ts` (`loadMcpServers`, `McpServerConfig`), `src/core/codex/CodexSession.ts` (the `thread/start` / `thread/resume` / `thread/fork` call sites and how `dynamicTools` is attached), the generated `ThreadStartParams` / `ThreadResumeParams` / `ThreadForkParams` (`config?: { [key]: JsonValue }`), `.cache/codex-src/codex-rs/config/src/mcp_types.rs` (`McpServerConfig`, `McpServerTransportConfig`: stdio `{ command, args, env }`, streamable HTTP `{ url, bearer_token_env_var, http_headers, env_http_headers }`; `deny_unknown_fields`, untagged), `src/integration/codex/codex-injection.integration.test.ts` (the two-platform fixture shape), the probe section of `docs/codex-spike.md`.

**Design (decided; do not reopen).**

1. _Probe first, in the repo._ Before wiring anything, add `src/integration/codex/codex-mcp-override.integration.test.ts` (macOS arm64 + Windows x64, unsandboxed on Windows, replacement environment, marker-writing node stub as in the spike) with three cases: (a) the spike's control/override pair; (b) a `config.toml` holding a NATIVE `mcp_servers.native` stub plus a `thread/start` override with `mcp_servers.probe` — record whether BOTH spawn (override merges into the table) or only `probe` does (override replaces the table); (c) the same with the override keyed as a dotted path `"mcp_servers.probe"` instead of a nested table. Whichever form preserves the native entry is the one the bridge sends; if neither does, stop and report — the design changes (the bridge would then have to read the native table through `config/read` and re-send it), and that is the reviewer's call.

2. _Translation._ `src/core/codex/codex-mcp-bridge.ts` with a pure `translateClaudeMcpServerForCodex(cfg): CodexMcpServerEntry | null` and `collectClaudeMcpForCodex(cwd): { servers: Record<string, CodexMcpServerEntry>; skipped: string[] }`. stdio (`type: 'stdio'`, or no type with a `command`) → `{ command, args, env? }`; HTTP (`type: 'http'`, or no type with a `url`) → `{ url, http_headers? }` (Claude's static headers map straight onto `http_headers`; there is no bearer-token field to derive). `type: 'sse'` has no Codex transport and is SKIPPED, returned in `skipped`. A config with neither command nor url is skipped silently as in the opencode bridge. Merge order and the disabled list are identical to `collectClaudeMcpForOpencode`; do not duplicate the I/O — factor the merge into a shared helper in `claude-mcp.ts` if the opencode bridge's is not already reusable. Never write to disk.

3. _Delivery._ `CodexSession` attaches `config: { mcp_servers: servers }` (or the dotted form, per the probe) to `thread/start`, `thread/resume` and `thread/fork` when `servers` is non-empty; nothing when empty. Once per session, when `skipped` is non-empty, emit one `session:warning` naming the skipped servers ("SSE MCP servers are not supported by Codex: a, b"). Headless dispatch targets get NO MCP servers (they get no dynamic tools either). Read at thread start only; a change to the Claude MCP config applies to the next session (the Codex settings page's row says so in Slice 5).

4. _Names._ Codex namespaces MCP tools as `mcp__<server>__<tool>` in `mcpToolCall` items; the existing tool map already renders them ("MCP tool server/tool"). No renderer change. Do not filter any name: ClaudeUI's hosted tools are dynamic tools on this engine, not MCP servers, so there is no `claudeui` reservation to protect.

**Guards.**

1. Translation table: stdio with/without args/env; http with/without headers; sse skipped; neither skipped; `type` absent inferred from command vs url.
2. Collection: user → project → local precedence, disabled-list removal, `skipped` reporting.
3. Delivery: `thread/start`, `thread/resume` and `thread/fork` carry `config.mcp_servers` (or the dotted form) exactly when the collection is non-empty; the dispatch target never does; one `session:warning` per session for skipped servers.
4. Integration: the three probe cases above on the real binary, run with `CODEX_INTEGRATION=1`; the merge/replace answer recorded in the test's doc comment and in `docs/codex-spike.md`.

**Gates.** typecheck, lint, full `bun run test`, `CODEX_INTEGRATION=1 bun run test:integration`, prettier on changed files. Reviewer: a real Codex session in a directory whose `.mcp.json` declares a stdio server shows that server's tools in a turn (or at least its startup notifications).

**Out of scope for Slice 4.** The Codex settings page's MCP row (Slice 5), MCP OAuth servers (native `config.toml` only), any writing of `config.toml`.

## Slice 4b: MCP tool approvals through the shared permission engine

**Landed 2026-09-14** on `codex-integration` (the commit after `37ccd411`). Two wire facts corrected against a request captured from the pinned binary (`src/core/codex/__tests__/fixtures/mcp-tool-approval-elicitation.json`): the elicitation carries NO question id (it is the MCP request id, consumed by the app-server), so the accept body is `{ action: 'accept', content: {} }`, which `parse_mcp_tool_approval_elicitation_response` maps to a plain approval; and a plain server's form names its tool only in the message (`Allow the <server> MCP server to run tool "<tool>"?`), parsed anchored on the params' `serverName`, falling back to the server-wide `mcp__<server>` scope. The discriminator is `_meta.codex_approval_kind === "mcp_tool_call"`. Plan mode DECLINES (the shared ladder denies kind `mcp` under plan for every engine; the kickoff's "ask" was wrong). The shared permission engine now matches `mcp__…` rules by tool name in every tier — before this, every such rule a user wrote was inert on pi too. Codex's persistence options are never echoed; a non-approval form is declined with one warning. Real-binary integration: the scripted model calls the inherited stub's tool, the app-server elicits, accept runs `tools/call` and the turn completes; a deny rule declines. Known cosmetic gap: `CodexEngineToolMap.kindOf` renders an `mcp__…` card through the generic body.

**Found live (2026-09-14).** With the inherited stub server running, the model's first MCP tool call ended as "user rejected MCP tool call". Codex 0.154.0 has no MCP-specific approval request: before an MCP tool runs (in every mode that asks — `untrusted`/`on-request` per ADR-067's mapping) the app-server sends the server request **`mcpServer/elicitation/request`** (`app-server-protocol/src/protocol/common.rs`, "Request input for an MCP server elicitation"; params `McpServerElicitationRequestParams { threadId, turnId, serverName, request: McpServerElicitationRequest }`, response `McpServerElicitationRequestResponse { action: accept | decline | cancel, content? }`). ClaudeUI does not register that method, the transport answers "unknown method", and `core/src/mcp_tool_call.rs` maps anything but Accept to `ReviewDecision::denied("user rejected MCP tool call")`. Until this slice every inherited MCP tool is unusable under the default mode.

**Goal.** Answer the MCP approval elicitation the way ClaudeUI answers Codex's command and file-change approvals (ADR-067): the shared permission engine decides from the merged `~/.claude` rules, with the tool named in Claude's MCP rule vocabulary `mcp__<server>__<tool>`; deny answers decline, allow answers accept, ask raises the standard `PendingApproval` card with always-allow suggestions in that vocabulary and "allow for this session" recorded as the shared session allow. Codex's own persistence options ("Allow and don't ask me again", the `_meta` persist key) are never sent: ClaudeUI owns rules and session allows.

**Read first.** ADR-067 (the mapping and "ClaudeUI decides"); `docs/architecture/codex.md` § Permissions; `src/core/codex/CodexSession.ts` (`requestApproval`, the `serverMethods` const, how `item/commandExecution/requestApproval` builds the card and answers `accept`/`decline`, how `allowForSession` records the shared key), `src/core/codex/codex-turn-policy.ts`, `src/core/pi/permission-engine.ts` (`hostedMcpKind`, how `mcp__*` names classify as kind `mcp`), `src/shared/codex-types.ts` (`CodexApprovalChoices`), the Codex source `core/src/mcp_tool_call.rs` (the constants `MCP_TOOL_APPROVAL_QUESTION_ID_PREFIX = "mcp_tool_call_approval"`, `MCP_TOOL_APPROVAL_ACCEPT = "Allow"`, `MCP_TOOL_APPROVAL_ACCEPT_FOR_SESSION`, `parse_mcp_tool_approval_elicitation_response`, `request_user_input_response_from_elicitation_content`, and `build_mcp_tool_approval_*` for what the form's `message`, `requestedSchema` and `_meta` carry — server, tool name, tool params), `protocol/src/approvals.rs` (`ElicitationRequest::Form`, `ElicitationAction`), `app-server-protocol/src/protocol/v2/mcp.rs` (the generated shapes), `scripts/generate-codex-protocol.mjs`, and `src/integration/codex/codex-mcp-override.integration.test.ts` (the stub server that now answers `tools/list`).

**Design (decided; do not reopen).**

1. _Protocol._ Generator `serverMethods` gains `'mcpServer/elicitation/request': ['McpServerElicitationRequestParams', 'McpServerElicitationRequestResponse']`; regenerate; `check-codex-protocol` passes.

2. _Recognise the approval._ In `CodexSession`, register the method in `serverMethods` and route it to a new `mcpElicitation(params, context)`. An elicitation is the MCP tool approval when its form's question id starts with `mcp_tool_call_approval` (read the exact field the Rust builder writes — the question id lives in the requested schema / meta; pin it with a fixture copied from a real request captured by the integration test). Extract `serverName` (params) and the tool name (meta); the gated tool name is `mcp__<serverName>__<toolName>`, the card's `toolName` is the same string and its `input` is the tool params from meta (display only).

3. _Decide._ Evaluate with the same engine and the same merged rules as the other Codex approvals, kind `mcp`: deny → `{ action: 'decline' }` plus the existing `session:error` naming the rule; allow (rule, session allow, or a mode whose base allows `mcp`) → `{ action: 'accept', content: { [questionId]: 'Allow' } }` (the content shape `request_user_input_response_from_elicitation_content` expects — confirm against the Rust and pin it); ask → the standard `PendingApproval` with `allowForSession` recording the shared key for `mcp__<server>__<tool>` and always-allow suggestions in that vocabulary; the human's allow answers accept/"Allow", deny answers decline. Plan mode: MCP tools are not plan-safe, so ask (consistent with the other engines' `mcp` kind under plan). Under `auto`, whatever reaches the client is gated exactly like `default` (ADR-067). Owning-turn end or disconnect aborts a pending elicitation through the existing `abortServerRequests` path; a cancelled card answers `{ action: 'cancel' }`.

4. _Other elicitations._ A form that is NOT the tool approval (an MCP server asking the user a real question) is answered `{ action: 'decline' }` with one `session:warning` naming the server ("<server> asked a question ClaudeUI cannot show yet"); rendering arbitrary elicitation forms is out of scope.

**Guards.** (1) Generator and `serverMethods` registration. (2) A recorded approval elicitation gates as `mcp__<server>__<tool>`: a user deny rule → decline + error; an allow rule → accept with the "Allow" content; no rule → a `PendingApproval` whose suggestions use the `mcp__` vocabulary, and the card's allow/deny/allow-for-session answers map to accept/decline/accept + shared session allow (the next identical call is allowed without a card). (3) Plan mode asks; auto gates like default. (4) A non-approval form is declined with one warning. (5) Turn end aborts a pending elicitation. (6) Integration, real binary: extend `codex-mcp-override.integration.test.ts` (or a sibling) so the scripted provider emits a function call to the inherited stub's tool, the app-server sends `mcpServer/elicitation/request`, the session answers accept, the stub's `tools/call` runs and the turn completes; and the same with a deny rule → decline and the model sees the rejection. If the mock model cannot be made to emit an MCP function call within reasonable effort, report exactly what blocked it and rely on the unit guards.

**Gates.** typecheck, lint, full `bun run test`, `CODEX_INTEGRATION=1 bun run test:integration`, `bun run check-codex-protocol`, prettier on changed files. Reviewer: the live check that failed today — a Codex session in `D:\WorkPlace\codex-scratch` asked to call `ping` on `verify-stub` shows the approval card, allow runs the tool, and the reply contains "pong-from-mcp".

**Out of scope.** Rendering arbitrary MCP elicitation forms; Codex-side persistence of MCP approvals; the settings page.

## Slice 5: settings

Split into 5a (the config service and the Codex engine page) and 5b (Codex on the topic pages, stale copy, rules recompile) so each is one reviewable diff. Visual reference: mockup screens 2 and 3 (owner-approved 2026-09-13); the one-home table is ADR-068 §6.

### Slice 5a: the Codex config service and the Engines › Codex page

**Landed 2026-09-14** on `codex-integration` (the commit after `463b060b`). Built as specified, with these refinements from review and four isolated-profile drives:

- _Probe answers_ (`docs/codex-spike.md` § "`config/batchWrite` probe", pinned by `src/integration/codex/codex-config-write.integration.test.ts`, macOS arm64 + Windows x64): `value: null` REMOVES a key, so Reset is a real removal and `modified` means "present in the base user layer"; a stale `expectedVersion` is refused with the camelCase tag `configVersionConflict` in `error.data.config_write_error_code`, which `CodexTransportError.nativeCode` now carries (a bare ASCII identifier, nothing else admitted); `batchWrite` does NOT validate a key against the schema, so a misremembered key is written silently and breaks the next session — every key on the page was therefore checked against `config/src/config_toml.rs`, not the ADR-068 §6 table. Two keys the ADR named do not exist on 0.154.0 (`browser_use.enabled`, `computer_use.enabled`); the Tools group writes `features.browser_use` / `features.computer_use`.
- _One process per click._ `CodexService.batchWriteConfigAndRead` runs the write and the read-back on ONE app-server child and the fresh snapshot rides back on the result, so the store folds it in instead of issuing a second `codex-config:read`. The service omits `filePath` (the binary's default IS the user `config.toml` under `CODEX_HOME`) and is built without an auth hook: a config edit is not an act of the account, so no vault read.
- _Found in the first live drive._ The write result is discriminated on `status`, not `ok`: the preload and web `unwrap` helpers treat ANY `{ ok }` object as the IPC envelope and handed the renderer `undefined` (three page errors, and the store's promise queue stayed rejected so every later click was swallowed). `codex-config.test.ts` pins that no branch carries an `ok` key, and `commit` now catches every throw so one bad write cannot poison the queue.
- _Two toggle defaults were inverted against the source_ and fixed in review with guards proven failing first: `shell_environment_policy.ignore_default_excludes` resolves `unwrap_or(true)` (`config/src/shell_environment_policy.rs`) so nothing is stripped by default, and `codex app-server` enables analytics only with `--analytics-default-enabled` (`cli/src/main.rs`), which ClaudeUI never passes — both rows render OFF when the key is absent. Also: `personality` is an enum (the kickoff's custom text is not a valid value); `service_tier` offers priority/flex (`"priority"` is honoured only with `features.fast_mode`); `model_verbosity` has a "Default" segment that removes the key; the `mcp_optional_startup_grace_ms` and `background_terminal_max_timeout` placeholders read "default" because their constants are not in the pinned checkout (and the field is 88px wide).
- _Raw config is READ-ONLY_, the kickoff's own fallback: `batchWrite` has no whole-file form and ClaudeUI must not re-emit TOML, so the view shows the user layer as JSON with the file path; keys without a row are edited in the file itself.
- _Wiring._ `open-mcp-servers` is a new window event: the MCP row dispatches it, `TopBar` opens the MCP dialog, and both settings hosts (`Sidebar/SettingsPanel`, mobile `SessionView`) close themselves on it. `LeafRow` / `StackedRow` / `ToggleRow` gained `indent` for the dependent rows. `codex:recompile-rules` calls `syncCodexRulesFile({ force: true })` and `codexRulesStatus()` reports path, rule count, skipped count, mtime and up-to-date for the Managed row.
- _Live_ (isolated `os.homedir()` shim plus a temp `CODEX_HOME` holding a fabricated `config.toml` with a comment and sibling tables; the app-server wrote its state and the rules file only into that temp home): all eleven groups render from the file, the Windows-only sandbox rows show on win32, and a select, a toggle, a segment and a chip each wrote exactly their key (`model_reasoning_summary`, `sandbox_workspace_write.network_access`, `model_verbosity`, `project_doc_fallback_filenames`) with the comment and `[features]` byte-intact; the rows read the written values back with the hover Reset and Raw config showed the user layer; no console errors. Not driven live: the version-conflict notice (unit guard plus integration case (b)). Observation for follow-up, pre-existing: the anchored select menu did not stay open when its trigger was clicked right after the dialog had scrolled to a lower group; it opens normally at the top of the page.

**Goal.** Codex's `config.toml` is read and written through the app-server (`config/read` with layers, `config/batchWrite` with `expected_version`), never parsed by ClaudeUI, and the Engines › Codex page shows the curated groups of ADR-068 §6 on the shared row vocabulary: Account (Slice 2a's row), Model behaviour, Context & compaction, Instructions, Workspace sandbox, Shell environment, Tools & search, Native agents, MCP servers (the inherited-list row from Slice 4 plus `mcp_optional_startup_grace_ms`), History & privacy, Managed, Raw config.

**Read first.** ADR-068 §6 and ADR-065 (row vocabulary; "two panes over one config need ONE config object"); `src/renderer/src/components/SettingsDialog/settings-pages.tsx` (`SettingsGroup`, `itemsOf`, the `SECTIONS` inventory), `settings-sections.tsx` (how `pi-config-*` and `opencode-*` items are declared), `PiConfigPanes.tsx` and `OpencodeConfigPanes.tsx` (`LeafRow`, `StackedRow`, `ToggleRow`, `LeafNumberInput`, `LeafTextInput`, `OpencodeManagedKeysSection`'s `locked` rows, `PiRawConfigSection`), `settings-controls.tsx`, the reference-counted external store the Dispatch page uses for one shared config object (ADR-065 § As built item 3), `src/core/ipc/config-commands.ts` (`config:patch-pi-native` / `config:patch-opencode-native` as the command shape), `src/core/codex/CodexService.ts` (`read()`, `effectiveConfig()`), `scripts/generate-codex-protocol.mjs`, the Codex source `app-server-protocol/src/protocol/v2/config.rs` (`ConfigReadParams { includeLayers, cwd }`, `ConfigReadResponse { config, origins, layers }`, `ConfigLayer { name, version, config, disabledReason }`, `ConfigLayerSource` — the `user` layer with `profile: null` is the base file, `ConfigBatchWriteParams { edits[{keyPath, value, mergeStrategy}], filePath?, expectedVersion?, reloadUserConfig }`, `MergeStrategy { Replace, Upsert }`, `ConfigWriteResponse { status, version, filePath, overriddenMetadata }`, `ConfigWriteErrorCode` — `ConfigVersionConflict`, `ConfigSchemaUnknownKey`, `ConfigValidationError`), and `config/src/config_toml.rs` for every key's type.

**Design (decided; do not reopen).**

1. _Protocol._ Generator selection gains `'config/value/write': ['ConfigValueWriteParams', 'ConfigWriteResponse']` and `'config/batchWrite': ['ConfigBatchWriteParams', 'ConfigWriteResponse']`; regenerate; `check-codex-protocol` passes.

2. _Probe first (real binary, isolated `CODEX_HOME`, add to `src/integration/codex/` as `codex-config-write.integration.test.ts`, two-platform like the others)._ (a) `config/batchWrite` with `mergeStrategy: 'replace'` on `model_verbosity` writes the key into the user `config.toml` and a following `config/read` reflects it with a new user-layer `version`; (b) a write with a stale `expectedVersion` fails with `ConfigVersionConflict`; (c) HOW A KEY IS REMOVED: try `value: null` with `replace` — record whether it deletes the key, writes `null`, or is refused (`ConfigValidationError`); if none removes the key, "Reset to default" for a scalar becomes "write the documented default" and the Reset affordance says so. (d) a nested table key path (`sandbox_workspace_write.network_access`) and an array (`project_doc_fallback_filenames`) round-trip. The answers fix the service's `remove` semantics; record them in `docs/codex-spike.md`.

3. _Core service._ `src/core/codex/codex-config.ts`: `readCodexConfig(): Promise<CodexConfigSnapshot>` = `config/read { includeLayers: true, cwd: homedir() }` through a `CodexService` read (injected hook as elsewhere), returning `{ version, file, profile, user: Record<string, JsonValue> (the base user layer's raw TOML as JSON), effective: Config, origins }`; `writeCodexConfig(edits: Array<{ keyPath: string; value: JsonValue | null }>, expectedVersion: string): Promise<{ version: string }>` = one `config/batchWrite` with `mergeStrategy: 'replace'`, `reloadUserConfig: true`, the base user file as `filePath` (from the layer source), mapping `null` to whatever the probe found removes a key. Errors surface by code: a version conflict becomes a typed error the UI turns into "config.toml changed on disk, reloaded" and re-reads; unknown-key and validation errors carry the native message verbatim. Commands `codex-config:read` (query) and `codex-config:write` (command), capability `config`, both transports, added to the pinned remote channel set. The raw user layer may contain MCP headers or env values the user put there; that is the same exposure the opencode and pi raw panes already have, and nothing here logs it.

4. _One config object._ A reference-counted external store (`use-codex-config.ts`, the Dispatch page's pattern) holds the snapshot for every Codex pane on the page; each write goes through it, re-reads after success, and on a version conflict re-reads and shows the notice once. Rows compute `modified` as "key present in the user layer"; Reset removes the key (or writes the default, per the probe).

5. _Groups and rows_ — `CodexConfigPanes.tsx`, items `codex-config-<group>` in `SECTIONS`, groups on the Codex page in this order with storage tag `config.toml` and `appliesOn: 'next-session'` unless noted:
   - **Account** — Slice 2a's row (unchanged).
   - **Model behaviour** — `model_reasoning_summary` (select: auto/concise/detailed/none), `model_verbosity` (segmented: low/medium/high), `plan_mode_reasoning_effort` (select from the native tiers the catalog publishes, blank = follow session), `service_tier` (segmented: default/priority/flex), `personality` (select: none/friendly/pragmatic + a custom text), `review_model` (select from catalog, blank = session model).
   - **Context & compaction** — `model_context_window`, `model_auto_compact_token_limit` (numbers, blank = catalog), `model_auto_compact_token_limit_scope` (segmented: total / after carried prefix, nested under the limit), `tool_output_token_limit`, `project_doc_max_bytes` (numbers), `project_doc_fallback_filenames` (chip list), `compact_prompt` (text, full width).
   - **Instructions** — `developer_instructions`, `instructions` (text areas), the four `include_*` toggles.
   - **Workspace sandbox** (badge `acceptEdits · Auto`, one explanatory row first as in the mock) — `sandbox_workspace_write.network_access` (toggle), `.writable_roots` (path chip list), `.exclude_tmpdir_env_var` and `.exclude_slash_tmp` (toggles, inverted as "Keep … writable"), `allow_login_shell` (toggle), `project_root_markers` (chip list), `windows.sandbox` (select) and `windows.sandbox_private_desktop` (toggle) shown only on win32.
   - **Shell environment** — `shell_environment_policy.inherit` (segmented core/all/none), `.ignore_default_excludes` (toggle, inverted as "Strip secret-looking variables"), `.exclude`, `.include_only` (chip lists).
   - **Tools & search** — `web_search` (segmented disabled/cached/indexed/live), `tools.update_plan.enabled`, `tools.experimental_request_user_input.enabled`, `browser_use.enabled`, `computer_use.enabled` (toggles), `background_terminal_max_timeout` (number, ms), one locked "Hosted tools" row linking to Mockups and Cross-engine dispatch.
   - **Native agents** — `agents.enabled` (toggle), nested `agents.max_concurrent_threads_per_session`, `agents.max_depth` (numbers), `agents.default_subagent_model` (select), `agents.default_subagent_reasoning_effort` (select).
   - **MCP servers** — the inherited-list row (count from `collectClaudeMcpForCodex(homedir())`, "Open MCP servers ›" dispatching the existing MCP dialog open event), a Native row listing the keys of the user layer's `mcp_servers` with a Raw config link, `mcp_optional_startup_grace_ms` (number, ms). Badge `Shared · all engines`.
   - **History & privacy** — `history.persistence` (segmented save-all/none), `analytics.enabled`, `feedback.enabled` (toggles), `thread_unload_delay_secs` (number, s; note "Next server start").
   - **Managed** (badge Locked) — locked rows for `model_provider = "openai"`, `check_for_update_on_startup = false`, the three policy keys (description names ADR-067), `profile` (shown when set, never written), and the rules file row (`~/.codex/rules/claudeui.rules`, rule count and last sync from `rules-sync.ts`, a Recompile action calling a new `codex:recompile-rules` command that runs `syncCodexRulesFile()` forced).
   - **Raw config** — one action row opening a `config.toml` editor sheet (the `PiRawConfigSection` pattern) that writes the whole user file through one `batchWrite` of the changed top-level keys (or, if the probe shows batchWrite cannot express a whole-file replace safely, a read-only view with the file path — say which).
     Page description becomes: "Codex's own configuration. Only the key you change is written to config.toml; other keys and comments are kept. Approvals, sandbox mode and the reviewer are set by the session's permission mode, not here."

6. _Inventory._ `SECTION_TARGET` / the inventory guard cover every new item key exactly once.

**Guards.** Service read picks the base user layer and reports version/file/profile; write sends one batchWrite with replace + reloadUserConfig + expectedVersion; version conflict maps to the typed error; null/removal per the probe; command registration and the pinned remote set; the shared store re-reads after a write and after a conflict; one representative row per control kind (toggle, select, segmented, number, chip list, text) writes the right key path and shows `modified` only when the key is in the user layer; win32-only rows hidden elsewhere; Managed rows locked; the MCP row shows the inherited count; the inventory guard passes; the integration probe cases (a)–(d).

**Gates.** typecheck, lint, full `bun run test`, `CODEX_INTEGRATION=1 bun run test:integration`, `bun run check-codex-protocol`, prettier on changed files. Reviewer: real-app drive against an isolated profile with a fabricated `~/.codex/config.toml` (through a temp `CODEX_HOME`): flip a toggle, change a select, add a chip, confirm the file changed by exactly those keys and the rows read back; open Raw config.

**Out of scope for 5a.** Codex segments on the topic pages, stale strings, `engines/codex.json` defaults (5b).

### Slice 5b: Codex on the topic pages, stale copy

**Landed 2026-09-14** on `codex-integration` (the commit after `3be8e0b2`). Built as specified, with these refinements from the implementer's own review and the reviewer's:

- _Default models._ `EngineConfig.codexConfig { defaultModel?, defaultEffort? }` in `engines/codex.json`; the store gained `codexDefaultModel` / `codexDefaultModelConfigured` / `codexDefaultEffort` and `setCodexDefaults`, hydrated at boot beside pi's. A configured default seeds a new Codex session with `codexModelExplicit: true` (ADR-059); a sticky pick still beats it; a configured model the catalog no longer lists leaves the picker unset and banners it by name. Deliberate departure from opencode/pi: an EMPTY Codex catalog keeps resolving to `null` even with a default configured, because that state has its own diagnosis (`vendorAuthProbe` → "Sign in again" vs "check the installation") which a named model would have swallowed.
- _Effort is PAIRED with the model_ (reviewer fix, guard proven failing with `expected 'xhigh' to be undefined`). The pane offers only the tiers the configured model publishes, so `defaultEffort` is a statement about that model: `codexDefaultEffortFor` sends it only when the fresh session's model IS the configured default (or, with no default model, when the catalog says the session's model publishes it, or the model is Codex's own). Without this a sticky pick of another model made `CodexSession.validateEffort` refuse every new session. Effort is never sent on a resume, which would undo a live `thread/settings/update`. A stale tier for the configured model shows a `staleEffort` warning row.
- _One config object for `engines/codex.json`._ The Dispatch page's reference-counted store became `useEngineConfigObject` / `updateEngineConfigObject`; the Default-models segment writes `codexConfig` through it, because settings search mounts panes from several pages at once and `saveEngineConfig` replaces the whole file. `useDispatchConfig` is a thin wrapper; opencode/pi behaviour unchanged.
- _Dispatch._ `ENGINE_ORDER` gained `codex`; Codex segments on Dispatch-into and Limits over `engines/codex.json#dispatch` (no opencode-only timeouts). `DISPATCH_CALLERS` was corrected for ALL four engines, not only `codex`: Codex hosts `dispatch_agent` as well as accepting it (ADR-033 slices E and H), so "a Claude or pi" on the opencode row had become stale too.
- _Judge._ `CodexAutoReviewSection` (in `CodexConfigPanes.tsx`, reusing its `ManagedRow` and `TextAreaRow`): a locked "Guardian / Native" row and `auto_review.policy` (`AutoReviewToml.policy: Option<String>`, checked in the source) written through the Slice 5a store; the judge card's `storage` and `note` became per-engine functions (`config.toml`, "Read once per thread").
- _Permissions._ `SettingRow.engine` accepts a list; the rules row shows Claude + Codex chips and a compiled-rules sentence, gated on `engineIsInstalled('codex')` because `syncCodexRulesFile` never writes without the binary.
- _Stale strings_ are guarded behaviourally through `noteOf` for every engine rather than by a source grep, since both constants are module-private.
- _Live_ (isolated profile, temp `CODEX_HOME`): Default models › Codex picked `gpt-5.6-luna` and `high` → `engines/codex.json` held exactly `codexConfig`, the effort select offered Luna's five tiers, and a session created through the store seeded Luna with `codexModelExplicit: true` and the pill read "GPT-5.6-Luna"; Dispatch › Codex listed the catalog with the corrected Limits note; the judge segment wrote `[auto_review] policy = …` into the fabricated `config.toml` with its comment intact; the permissions row showed `claude,codex` chips and the rules sentence. No console errors. Not driven: a real Codex turn on the configured default (the `session:create` arguments are pinned by the InputBox test, and `CodexSession.start` has carried `model` and `effort` since Slice 2).
- _Pre-existing, noted, not fixed:_ `PiSessionDefaultModel` holds its own `useState` copy of `engines/pi.json` beside the Dispatch store's (the clobber hazard closed here for Codex); a pre-spawn Codex session's effort picker routes to `session:set-effort` on a session with no backend.

- **Default models, Codex segment.** `EngineConfig.codexConfig?: { defaultModel?: string; defaultEffort?: string }` in `engines/codex.json`, following `piConfig.defaultModel`'s precedent through `engine-meta.ts` (`defaultModelValue`) so a new Codex session starts on it with `codexModelExplicit` true (a configured default IS an explicit choice, ADR-059), blank = inherit the working directory's native `model` (today's behaviour). Effort likewise, from the catalog's native tiers. `DEFAULT_MODEL_NOTES.codex` becomes "Applies to new Codex sessions."; the orphan guard covers a configured default the catalog no longer lists.
- **Cross-engine dispatch, Codex segment.** `ENGINE_ORDER` gains `codex`; the Dispatch-into and Limits groups get `codex` item lists over `engines/codex.json#dispatch` (already read by the dispatcher); `DISPATCH_CALLERS.codex` becomes "a Claude, opencode or pi".
- **Sessions & autonomy › Auto-mode judge, Codex segment.** One locked "Guardian" row (native `auto_review` reviewer, ADR-067) and `auto_review.policy` as a text row written through Slice 5a's config service; note "Read once per thread." with `appliesOn: 'next-session'`.
- **Sessions & autonomy › Permissions.** The Bash rules row gains a Codex chip and one sentence about the compiled rules file.
- Guards: each segment renders and writes to its store; the inventory guard; a spawn honours `codexConfig.defaultModel`; the two stale strings are gone (grep-level test).
- Gates as 5a. Reviewer: settings drive against the isolated profile for the three segments; one Codex session started with a configured default model.

## Slice 6: sign-in entry points on the model picker and the composer

**Landed 2026-09-14** on `codex-integration` (the commit after `015616ad`). Built as specified, with these refinements:

- _Registry plumbing._ `ProviderAccounts.list` entries gained `needsReauth` (a boolean the vault already publishes; never token material), because a stored account counts as the credential and a revoked refresh token otherwise reads `connected` forever. `CHATGPT_ROUTE_PROVIDER_IDS` in `shared-provider.ts` is the one home for the route ids (pi `openai-codex`, opencode `openai` — confirmed in `SharedProviderRepository.chatgptProvider`) and the repository uses it too.
- _`refreshProviderAuth` is hooked once_, in `ProviderList.reload`, which every sheet write funnels through, and takes the snapshot the list just read rather than re-listing (`provider-registry:list` can start an opencode server). Also called from `hydrateConfigFromDisk` (outside the boot `Promise.all`, so the slow read never delays boot) and `closeSignIn`.
- _`providerAuth` has two writers on disjoint keys_ (`setVendorAuth` derives `anthropic`; `refreshProviderAuth` writes `chatgpt`/`chatgptRoutes`), pinned by a guard; a store-level selector composing the two would remove the hazard for tests that set `vendorAuth` directly — noted, not built.
- _The picker reads the store_ (`providerAuth`, `openSignIn`), so every `ModelPicker` call site — composer, settings, automation — gates the same way; defaults are `'unknown'`, which renders as before. The mobile ModelPage reuses the desktop `ModelPicker.signIn` testid on purpose (the two never co-render); the mobile sheet stays open behind the full-screen dialog.
- _Composer hint_ testids: the row `InputBox.signInHint` and its button `InputBox.signInHint.action`, both with `data-id` = provider. Codex's hint resolves through `engineMeta('codex').defaultVendorId` (`openai`) when the placeholder model carries no vendor.
- _Provider names unified in review:_ the entry points said "Anthropic" while the dialog says "Claude"; `SIGN_IN_PROVIDER_LABEL` (`Claude` / `ChatGPT`) is now the one table and `SignInDialog` imports it.
- _Live_ (isolated profile): with NO ChatGPT account the Codex composer showed "Codex needs a ChatGPT sign-in.", the picker listed "Sign in to ChatGPT" above six dimmed models, and the click opened the dialog headed "Sign in to ChatGPT"; with a fabricated connected account (`auth-vault.json` v3, unsigned JWT-shaped token) the auth view read `authenticated`, pi's picker showed eight models with no Sign in item and no dimming, and no hint. Codex itself drops out of the engine picker under a fake token because its catalog read fails — pre-existing capability honesty, not this slice. No console errors on any drive.

Deferred from Slice 3 ("neither surface knows the auth state today"). Visual reference: `mockup.html` at the repo root (untracked, owner-approved 2026-09-13), screen 5's notes: "Model picker: greyed models whose provider is unauthenticated carry a Sign in item that opens the dialog" and "Welcome screen: engine tile without a usable account shows Sign in instead of Start". The real app has no engine tiles — the engine is chosen in the composer's `EnginePicker` on a session that has not spawned — so the second entry point lands on the composer.

**Read first.** `docs/adr/adr-068_…` §3, this spec's Slice 3 (Landed + design: `openSignIn`, `SignInRequest { providerId: 'anthropic' | 'chatgpt', mode }`, `signInDialog` / `closeSignIn` in `session-store.ts`), `src/renderer/src/components/auth/SignInDialog.tsx`, `AuthBanner.tsx` (how it reads `vendorAuth.anthropic`), `src/renderer/src/hooks/useClaudeEvents.ts` (`session:auth-source` → `setVendorAuth`), `src/shared/provider-registry.ts` (`ProviderRegistrySnapshot`, `ProviderEntry { id, credential, engines: Partial<Record<EngineId, ProviderEngineFacts>>, accounts }`; the ChatGPT row is `id === 'chatgpt'`, `credential` is `'connected'` or `'none'`, `accounts.list[].needsReauth`), `window.api.listProviderRegistry()` and its consumers `ProviderList.tsx` / `ProviderSheet.tsx`, `src/renderer/src/components/shared/InlinePickers.tsx` (`ModelPicker`, `deriveModelGroups` — groups are (engineId, vendorId) pairs — `EnginePicker`), `src/renderer/src/components/chat/InputBox/{InputBox.tsx,View.tsx,MobileConfigSheet.tsx}` (`InputBox.statusLine`, the mobile ModelPage also uses `deriveModelGroups`), `src/shared/engine-meta.ts` (`defaultVendorId`), and `ADR-030` (never advertise what does not work).

**Design (decided; do not reopen).**

1. _One renderer-side auth view._ `session-store.ts` gains `providerAuth: { anthropic: AuthState; chatgpt: AuthState; chatgptRoutes: Partial<Record<EngineId, boolean>> }` (`AuthState` = `'authenticated' | 'unauthenticated' | 'unknown'`, from `src/shared/types.ts`). `anthropic` is DERIVED from the existing `vendorAuth.anthropic` (unknown until a Claude session has reported `session:auth-source`; never claim unauthenticated on unknown). `chatgpt` comes from `listProviderRegistry()`: `'authenticated'` when the ChatGPT entry's `credential === 'connected'` and the active account does not need re-auth, `'unauthenticated'` when the entry exists with `credential === 'none'` or the active account `needsReauth`, `'unknown'` before the first read or when the read fails; `chatgptRoutes[engine] = entry.engines[engine]?.enabled ?? false`. One action `refreshProviderAuth()` runs the read; call it from `hydrateConfigFromDisk`, from `closeSignIn` (the dialog's outcome changes the answer), and after every registry mutation the settings sheets perform (`ProviderList`/`ProviderSheet`/`ProviderAddSheet` already re-list after add/remove/switch/disconnect — call the store action there too, once each). No new IPC channel: `provider-registry:list` is already registered on both transports.

2. _Which provider gates which picker group._ A pure helper `signInProviderFor(engineId, vendorId, providerAuth): { providerId: SignInProviderId; state: AuthState } | null` in a new `src/renderer/src/utils/sign-in-provider.ts`: claude + vendor `anthropic` → anthropic; codex (its vendorId is `openai`) → chatgpt; pi and opencode → chatgpt ONLY for the vendor that IS the ChatGPT route (`openai-codex`, `engine-meta.ts`'s pi `defaultVendorId`; confirm opencode's route vendor id from `provider-registry.ts`'s derivation rather than assuming) and only when `chatgptRoutes[engine]` is true; every other vendor (API keys, free, custom) → null. Unit-tested on its own.

3. _Model picker._ In `ModelPicker` (desktop) and the mobile ModelPage, a group whose provider resolves to `state === 'unauthenticated'` renders its options DIMMED (`opacity-60`, still selectable — a user may pick the model and then sign in) and gains ONE leading item `Sign in to <Anthropic|ChatGPT>` (testid `ModelPicker.signIn`, `data-id` = provider id) which calls `openSignIn({ providerId, mode: 'reauth' })` and closes the picker. `'unknown'` renders exactly as today. The picker takes `providerAuth` through a prop or the store hook — do not read `window.api` from the picker.

4. _Composer hint (the mock's tile)._ On a session that has NOT spawned (`!status.sessionId && !isHistorical && no messages`) whose selected engine's default provider resolves to `'unauthenticated'` (claude → anthropic; codex → chatgpt; pi/opencode → chatgpt only when the selected model's vendor resolves to chatgpt per item 2), render one compact row above the composer: `<Engine label> needs a <Provider> sign-in.` with a `Sign in` link button (testid `InputBox.signInHint`, `data-id` = provider id) that opens the dialog in `reauth` mode. It disappears the moment `providerAuth` says authenticated or the session spawns. The send button is NOT disabled (sending still yields the Slice 3 auth-required row; ADR-030's rule is about advertising, not blocking). Desktop and mobile composer both.

5. _Nothing else changes._ AuthBanner, AuthRequiredRow, the settings sheets keep their entry points; the dialog is unchanged.

**Guards (each proven failing first).** `signInProviderFor` table (claude/anthropic, codex/openai, pi/openai-codex with route on and off, opencode's route vendor, an API-key vendor → null). `refreshProviderAuth` maps a registry snapshot to the three states (connected / none / needsReauth / read failure → unknown) and is called on hydrate and on `closeSignIn`. `ModelPicker`: an unauthenticated group shows the Sign in item and dimmed options, clicking it calls `openSignIn` with the provider and closes; an authenticated or unknown group shows neither. Composer hint: shown only pre-spawn and only for unauthenticated; hidden after spawn (`status.sessionId` set) and when unknown; the click opens the dialog in `reauth`. Mobile ModelPage mirrors the desktop assertion. Existing InputBox, picker, SignInDialog and settings tests stay green.

**Gates.** typecheck, lint, full `bun run test`, prettier on changed files, `git diff --check`. Reviewer: isolated-profile drive with NO ChatGPT account in the fabricated vault → on a fresh Codex session the composer hint and the picker's Sign in item render; clicking opens `SignInDialog` (not completed); a fabricated connected account hides both.

**Out of scope.** Completing a sign-in in tests or drives; the headless-server device code; pi's auth event; engine tiles (none exist).

## Slice 7: device-code sign-in for ChatGPT on remote clients

**Landed 2026-09-14** on `codex-integration` (the commit after `1adbbe23`). Built as specified after three review rounds; the refinements:

- _The wait is host-owned._ The first build awaited the poll through `vendor-auth:oauth-callback`; the web connection caps every invoke at 30 s (`INVOKE_TIMEOUT_MS`, `src/web/connection.ts`), so the flow would have died on the one client it serves. As built: `vendor-auth:device-code-start` starts the host-side wait at once (`PiAuthProvider` kicks `credentialSync.completeLogin()` unawaited and records a single-flight `DeviceCodeWait`), and the client polls the new `vendor-auth:device-code-status` query (`{ state, error? }`; `cancelled` also answers when no flow is live, so a reloaded client stops polling). The store never calls `oauth-callback` on this path — pinned by a guard.
- _A new start supersedes a live device flow._ `AuthVault.claimLoginSlot()` cancels a live `device` flow for either kind of start and still refuses a live `loopback` one; `CodexDeviceCodeFlow.waitForCompletion` races the run against the abort signal so a cancel settles the caller whatever an injected fetch or sleep does. The poll interval accepts a number or a string and is floored at 1 s (Rust defaults a missing interval to 0); the 15-minute deadline is anchored at `start()` so the UI's countdown is the host's deadline.
- _The web client never refreshed the auth view_ (a Slice 6 gap the drive exposed): `hydrateConfigFromDisk` is desktop-only, so on the headless server `providerAuth` stayed `'unknown'` and the picker item and composer hint never appeared. `src/web/main.tsx` now calls `refreshProviderAuth()` once after the first replica hydration (not on re-syncs, which fire on every phone foreground and the read can start an opencode server). Known divergence: `boot-test-app.ts` mirrors the handler without the call.
- _The headless server refused every engine but Codex_ ("Engine sign-in is not available on the headless server yet"). `src/server/engine-auth.ts` now serves `pi` (the vault's provider, Electron-free, `credentialSync` already running there) and `codex`, and refuses `claude` and `opencode` with a message that says why. Residual: pi's PKCE fallback on the server binds the loopback on the server box's port 1455 for its 5-minute timeout — harmless for paste-back, but two concurrent fallbacks would collide; device code has no such port.
- _Panel_: `DeviceCodeFlow` (URL button, monospace code with guarded clipboard Copy, "Expires in N min" rounded to the nearest minute after the drive showed "16 min" for a fresh code, Cancel, "Paste the callback URL instead" which cancels host-side first). Dialog `busy` clears once the host answers so Copy is live during the wait. `security.md` § "The vendor-credential surface" carries the flow.
- _Live_ (hermetic `claudeui-server` on a scratch profile, password login, Playwright): from Settings › Models & providers › ChatGPT › Manage › Sign in, and from the Codex composer hint, the dialog showed a real user code from `auth.openai.com` with the verification URL and expiry, Copy and the paste fallback present, no page errors; the host polled while the panel waited; Cancel cleared the flow. No sign-in was completed. Residual: after Cancel with no stored account the dialog keeps the flow panel in its pre-code look (the dialog's existing "the user closes it" rule); a second look at that state is a small follow-up.

ADR-068 §3 reserved device code for the headless server: "ported from Codex's own `deviceauth/usercode` and `deviceauth/token` flow against the same issuer into the vault, not run through a throwaway native login." Paste-back (ADR-057) already completes a ChatGPT sign-in from any browser, but on a phone the step that hurts is copying a dead page's address bar. Device code replaces it with "open this link, type this code" while the host does everything else.

**Read first.** ADR-068 §3 and its As-built; ADR-057; `docs/architecture/security.md` § "The vendor-credential surface"; the Codex source `.cache/codex-src/codex-rs/login/src/device_code_auth.rs` (the whole file, 242 lines — the wire is there: `POST {issuer}/api/accounts/deviceauth/usercode {client_id}` → `{device_auth_id, user_code|usercode, interval}` (interval may be a string; read `deserialize_interval` for the default), verification URL `{issuer}/codex/device`, `POST {issuer}/api/accounts/deviceauth/token {device_auth_id, user_code}` polled every `interval` seconds where 403 and 404 mean "not yet", any other non-2xx fails, 2xx returns `{authorization_code, code_verifier, code_challenge}`, a 15-minute cap, then the ordinary `authorization_code` exchange at `{issuer}/oauth/token` with `redirect_uri = {issuer}/deviceauth/callback` and the SERVER-supplied verifier); `src/core/auth/vault/codex-oauth.ts` (`CLIENT_ID`, `ISSUER`, `OAuthDeps`, `exchangeCodeForTokens(code, redirectUri, pkce, deps)`, `buildVaultCredential`, `CodexLoginFlow` and the `LoginFlow` contract, `parsePastedCallback`); `src/core/auth/vault/CredentialSync.ts` (`completeLogin`, `completeVaultLoginFromPaste`, `cancelLogin`, `keyForCredential`, `feedAll`, `scheduleRefresh`) and `AuthVault.ts` (`completeLogin` → `upsertAccount`); `src/core/auth/PiAuthProvider.ts` (`oauthAuthorize` / `oauthCallback` / `cancelVendorOauth`); `src/core/ipc/auth-commands.ts` (`vendor-auth:oauth-authorize` / `-callback` / `-cancel`, `isRemote(connection)`, `safeHandler`) and the pinned remote set in `src/main/ipc/__tests__/remote-handlers.ipc.test.ts`; `src/preload/index.ts`, `src/web/api-adapter.ts` (+ its WIRED test), `src/test/helpers/boot-test-app.ts`; the renderer: `session-store.ts` (`VendorOAuthState`, `authorizeVendorOAuth`, `submitVendorOAuthCode`, `cancelVendorOAuth`), `src/renderer/src/components/auth/SignInDialog.tsx` (the `isWeb` branch and `OAuthPasteBackFlow`), `OAuthPasteBackFlow.tsx`.

**Design (decided; do not reopen).**

1. _Flow module._ `src/core/auth/vault/codex-device-code.ts` exports `CodexDeviceCodeFlow` with `start(): Promise<{ verificationUrl: string; userCode: string; expiresAt: number }>` (the usercode request; `expiresAt = now + 15 min`), `waitForCompletion(): Promise<VaultCredential>` (poll → exchange → `buildVaultCredential`), `cancel()` (aborts the in-flight fetch and the sleep; a cancelled wait rejects with a typed cancellation), `isSettled()`. Deps injectable like `CodexLoginFlow`: `OAuthDeps` (fetch, issuer) plus `now` and `sleep`, so the unit suite drives every branch with a fake clock and never touches `auth.openai.com`. The poll body and the usercode body are exactly Codex's; the interval defaults as the Rust does and is floored at 1 s. Token material never leaves the module except as the returned credential.
2. _Vault seam._ `CredentialSync` gains `beginDeviceCodeLogin(): Promise<{ verificationUrl; userCode; expiresAt }>` and routes `completeLogin()` (no pasted input) to the LIVE flow's completion — loopback or device code, whichever `beginLogin` / `beginDeviceCodeLogin` started last; `cancelLogin()` cancels either. The post-completion tail of `completeLogin` (generation check, `keyForCredential`, `needsReauth` reset, `feedAll` when active, `scheduleRefresh`, `startWatchers`) is shared, not duplicated: extract it into one private method both paths call. The credential is stored through the SAME vault upsert the loopback path uses (`AuthVault.completeLogin`'s tail — find the exact seam and reuse it; do not write the vault file from the new module). `AuthVault`'s `LoginFlow` factory may need a sibling for the device flow; keep both behind the vault's existing injection points so tests use fakes.
3. _Provider and commands._ `PiAuthProvider` gains `deviceCodeStart(vendorId)` for `openai-codex` only (the same vendor gate as `oauthAuthorize`); `oauthCallback(vendorId, method, '')` (empty code) keeps calling `credentialSync.completeLogin()`, which now completes whichever flow is live; `cancelVendorOauth` unchanged. TWO new channels in `auth-commands.ts` (same capability and `safeHandler` as `vendor-auth:oauth-authorize`): `vendor-auth:device-code-start` (command) returns `{ verificationUrl, userCode, expiresAt }` — never a token, never `device_auth_id` (the client does not need it; the host holds it) — and STARTS the host-side wait at once (the provider kicks `credentialSync.completeLogin()` in the background and records the outcome in one single-flight holder); `vendor-auth:device-code-status` (query) returns that holder's `{ state: 'pending' | 'done' | 'error' | 'cancelled', error? }`. The wait is NOT the `vendor-auth:oauth-callback` verb: the web connection caps every invoke at 30 s (`INVOKE_TIMEOUT_MS`, `src/web/connection.ts`) and a socket drop would lose a pending invoke, whereas host-owned state survives both. Register both on both transports (`preload`, `api-adapter` + WIRED test, `boot-test-app`), add to the pinned remote set. Not refused for any caller: the desktop may use it too, though its dialog does not offer it.
4. _Store._ `VendorOAuthState.stage` gains `'device-code'` with `verificationUrl`, `userCode`, `expiresAt`; new action `authorizeVendorDeviceCode(engineId, vendorId)` = start → set the stage → poll `vendor-auth:device-code-status` every 3 s (injectable timer) until `done` (run the same success path `submitVendorOAuthCode` runs: `refreshProviderAuth`, clear), `error` (set `stage: 'error'` with the message) or `cancelled` / a superseded flow token (silent). Never `vendorAuthOauthCallback` on this path. `cancelVendorOAuth` also covers it.
5. _Dialog._ On the web client (`isWeb`) the ChatGPT flow panel DEFAULTS to device code: a `DeviceCodeFlow` panel (new, in `OAuthPasteBackFlow.tsx` or a sibling) showing (1) "Open this link on any device" with the verification URL as an `<a target=_blank>` button, (2) the user code in large monospace with a Copy button (`navigator.clipboard`, guarded), (3) "Waiting for you to enter the code…" with the expiry as "expires in N min", (4) a Cancel button, and (5) one secondary link "Paste the callback URL instead", which cancels the device flow and starts the existing PKCE flow into `OAuthPasteBackFlow` exactly as today. The Anthropic flow is untouched (cli.js owns it). Desktop host: unchanged (loopback). Errors render through `OAuthOutcomeNotice`. Testids per ADR-027: `DeviceCodeFlow`, `DeviceCodeFlow.url`, `DeviceCodeFlow.code`, `DeviceCodeFlow.copy`, `DeviceCodeFlow.cancel`, `DeviceCodeFlow.pasteInstead`.
6. _Security._ No IPC result carries a token, a refresh token, `device_auth_id` or a client secret (there is none); the user code is display material. The 15-minute cap is enforced host-side; a new start of EITHER kind while a device flow is live cancels that device flow and takes the vault's login slot (`AuthVault.claimLoginSlot`), so a reload or a second tab mid-wait is never stuck behind it; a live loopback flow still refuses a second start. `docs/architecture/security.md` § "The vendor-credential surface" gains one paragraph describing the flow and what crosses the wire.

**Guards (each proven failing first).** Flow: usercode request body and headers; interval as number, as string, missing → default, `0` → floored; poll 403 then 404 then 2xx → exchange called with `{issuer}/deviceauth/callback`, the server verifier and the `authorization_code` grant, credential expiry from `expires_in`; a 500 fails at once with the status; the 15-minute cap with a fake clock; `cancel()` during the sleep and during a fetch rejects with the cancellation and no exchange happens. CredentialSync: device completion upserts through the vault and feeds when the account is active, not when it is not; `cancelLogin` cancels a device flow; a stale generation is dropped. Commands: registered on both transports, in the pinned set, the start result has exactly the three keys. Store: the stage transitions and the error/cancel paths. Dialog: web default shows URL, code, copy, cancel and the paste-instead link; the link switches to the paste panel; desktop shows no device panel. Existing auth tests stay green.

**Gates.** typecheck, lint, full `bun run test`, prettier on changed files, `git diff --check`. Reviewer: hermetic `claudeui-server` + browser walk: the dialog on the web client shows a real user code from `auth.openai.com` (the usercode request creates nothing and signs nothing in) and Cancel clears it; no real sign-in is completed.

**Out of scope.** Anthropic device code (cli.js owns that flow); opencode vendors; changing the desktop's loopback flow; completing a real login in tests or drives.
