node_modules
dist
out
.DS_Store
.eslintcache
*.log*
test-prompt.txt
.claude/settings.local.json
.claude/ui/mockups
*.tsbuildinfo
bin/

# Vendored CLI bundle — regenerated from the upstream Bun binary via
# scripts/extract-cli.mjs. Cached locally, rebuilt when claudeCliVersion
# bumps in package.json.
/vendor/claude-cli/
# Vendored opencode binary — downloaded via scripts/ensure-opencode.mjs.
/vendor/opencode-cli/
# Engine source checkouts (vendor/<engine>-src/, pinned to the vendored
# binary's tag) for reading upstream behavior — see CLAUDE.md.
/vendor/*-src/
/.cache/

# pi coding agent standalone build (vendored by ensure-pi)
/vendor/pi-cli/
/vendor/codex-cli/
/review.md
.claude/worktrees/
